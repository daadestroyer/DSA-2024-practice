package Stack_Queue_10.Implementation;

public class App05_ImplementStackUsingLinkedList {
    /*
        class MyStack {
             class StackNode {
             int data;
             StackNode next;
             StackNode(int a) {
                  data = a;
                  next = null;
             }
       }
       StackNode top;

       // Function to push an integer into the stack.
       void push(int a) {
            StackNode st = new StackNode(a);
            st.next = top;
            top = st;
       }

       // Function to remove an item from top of the stack.
       int pop() {
            if(top == null) return -1;
            int pop = top.data;
            top = top.next;
            return pop;
       }
}
    */
}
