package LinkedList_09.Problems;

public class App05_ReverseSinglyLinkedList {
    public static void main(String[] args) {
        /*
            public Node reverseList(Node head){
                if(head == null || head.next != null){
                    return head;
                }
                Node newhead = null;
                Node curr = head;

                while(curr != null){
                    Node fast = curr.next;
                    curr.next = newhead;
                    newhead = curr;
                    curr = fast;
                }
                return newhead;
            }
        */
    }
}
