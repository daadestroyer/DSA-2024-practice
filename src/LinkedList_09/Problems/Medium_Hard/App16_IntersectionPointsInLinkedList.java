package LinkedList_09.Problems.Medium_Hard;

public class App16_IntersectionPointsInLinkedList {
    public static void main(String[] args) {
        /*
            public Node getIntersectionNode(ListNode headA, ListNode headB) {
                   Node left = headA;
                   Node right = headB;

                   while(left != right){
                        if(left == null){
                            left = headB;
                        }
                        else{
                            left = left.next;
                        }
                        if(right == null){
                            right = headA;
                        }
                        else{
                        right = right.next;
                        }
                   }
                   return left;
                   // in short
                   // left = left == null  ? headB : left.next;
                   // right = right == null ? headA : right.next;
            }
        */
    }
}
