package LinkedList_09.Problems.Medium_Hard;

public class App13_DeleteTheMiddleNodeOfLinkedList {
    public static void main(String[] args) {
        /*
            public Node deleteMiddle(Node head){
                if(head == null || head.next == null){
                    return null; // as if only one node present we deleted that and returned null as no list present
                }
                Node slow = head;
                Node fast = head;
                Node prev = null;

                while(fast != null && fast.next != null){
                    prev = slow;
                    slow = slow.next;
                    fast = next.next;
                }

                prev.next = slow.next;
                slow = null;
                return head;
            }
        */
    }
}
